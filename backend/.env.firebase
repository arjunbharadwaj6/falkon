# Server Configuration
PORT=5000
NODE_ENV=development

# Firebase Configuration
# For production: Add your Firebase service account key as a JSON string
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"falkon-b7c5f",...}

# For development: Use application default credentials
# Run: firebase login
# Or set GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Email Configuration
EMAIL_SERVICE=zoho
EMAIL_USER=falkon@falkon.tech
EMAIL_PASSWORD=your-password-here
EMAIL_FROM=falkon@falkon.tech
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_SECURE=true

# Admin Email for approvals
ADMIN_EMAIL=admin@falkon.tech

# Frontend URL (for email links)
API_PUBLIC_URL=http://localhost:5000

# CORS Configuration
CORS_ORIGIN=http://localhost:5173,https://your-production-domain.com
